name: Ansible Syntax Checking

on:
  push:
    branches:
      - main

jobs:
  syntax-check:
    runs-on: self-hosted

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Create vault password file
        run: echo -n "${{ secrets.VAULT_PASSWORD }}" > /tmp/.vault_pass && chmod 600 /tmp/.vault_pass

      - name: Run Ansible syntax check
        env:
          ANSIBLE_VAULT_PASSWORD_FILE: /tmp/.vault_pass
        run: |
          cd $GITHUB_WORKSPACE
          for playbook in playbooks/*.yml; do
            echo "Checking $playbook..."
            ansible-playbook "$playbook" --syntax-check
          done

      - name: Remove vault password file
        run: rm /tmp/.vault_pass
        if: always()
